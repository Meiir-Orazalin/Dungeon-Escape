{
  "name": "dungeon-escape",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "A responsive, browser-playable dungeon movement prototype.",
  "scripts": {
    "start": "node scripts/serve.mjs",
    "test": "node --test",
    "check": "node --check src/game.js && node --check src/main.js && node --check scripts/serve.mjs && npm test"
  },
  "engines": {
    "node": ">=20"
  }
}
