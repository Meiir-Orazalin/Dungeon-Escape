export const BONE_STALKER_CONFIG = Object.freeze({
  movementSpeed: 105,
  closeDistance: 30,
});

export const ASH_WISP_CONFIG = Object.freeze({
  movementSpeed: 75,
  preferredMinimumDistance: 150,
  preferredMaximumDistance: 210,
  shotTelegraphMs: 350,
  shotCooldownMs: 2_000,
  initialShotDelayMs: 700,
  projectileSpeed: 190,
  projectileLifetimeMs: 2_200,
});

export const STONE_WARDEN_CONFIG = Object.freeze({
  movementSpeed: 55,
  chargeTriggerRange: 240,
  chargeWindUpMs: 550,
  chargeSpeed: 300,
  chargeDurationMs: 420,
  recoveryMs: 900,
});

export const ENEMY_RETURN_DISTANCE = 5;
